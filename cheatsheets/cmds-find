-name \*"something_between"\*		# use wildcards
-iname '*.gif'				# find extensions
-type d					# find only directories
-type -size +4M				# find only files large then ...
-maxdepth 1				# limit the recursive directory search
-executable				# find only executable files
-user ID				# only files of a specific user
-mtime 0				# only files modified within the last 24h
-newer REFERENCE			# only files newer then a referenced file

============ XARGS ============
find $(pwd) -name HipChat -print0 | xargs -0 rm
